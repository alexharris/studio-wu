import project from './project'
import about from './about'
import contact from './contact'
import press from './press'
import settings from './settings'

export const schema = {
  types: [project, about, contact, press, settings],
}
